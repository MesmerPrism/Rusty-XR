# Public provenance metadata for Rusty XR utility extraction.

format_version = 1

[[utility]]
crate = "rusty-xr-contracts"
source_family = "framework-neutral XR contract extraction"
extraction_date = "2026-04-29"
public_safe_rationale = "Plain camera, depth, hand, layer, interaction, render, timing, and view contracts are reusable across app shells and examples."
license_assumptions = "Authored for this MIT repository; upstream concepts are treated as behavioral references unless separately licensed and attributed."
scrubbed_private_dependencies = ["package identity", "downstream visual effect stacks", "app-specific scene behavior", "generated captures"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-runtime-config"
source_family = "cross-shell runtime profile workflows"
extraction_date = "2026-04-29"
public_safe_rationale = "Generic runtime keys and typed values let app shells map private aliases onto public configuration without publishing those aliases."
license_assumptions = "Authored for this MIT repository."
scrubbed_private_dependencies = ["downstream Android properties", "release profile names", "study-specific keys"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-ble"
source_family = "Android and Windows BLE workflow notes"
extraction_date = "2026-04-29"
public_safe_rationale = "UUIDs, GATT paths, operations, scan snapshots, and Android permission models are general protocol contracts."
license_assumptions = "Authored for this MIT repository from public BLE concepts and local workflow knowledge."
scrubbed_private_dependencies = ["device identities", "Bluetooth adapter handles", "runtime permission UI code"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-lsl"
source_family = "Lab Streaming Layer data-model workflows"
extraction_date = "2026-04-29"
public_safe_rationale = "Stream descriptors, channel schemas, endpoint status, telemetry, and staleness helpers are backend-neutral."
license_assumptions = "Authored for this MIT repository; native liblsl remains optional and external."
scrubbed_private_dependencies = ["downstream stream names", "native lsl runtime path", "study session identifiers"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-osc"
source_family = "OSC live-control and sensor-ingress workflows"
extraction_date = "2026-05-02"
public_safe_rationale = "OSC packet models, common argument encoding, endpoint status, and UDP transport helpers are reusable across app shells without publishing app-specific address trees."
license_assumptions = "Authored for this MIT repository from the public OSC 1.0 protocol shape."
scrubbed_private_dependencies = ["downstream stream names", "sensor-specific address maps", "app-specific command behavior"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-broker-model"
source_family = "research XR broker, sidecar stream, and clean-room low-latency transport workflows"
extraction_date = "2026-05-09"
public_safe_rationale = "Broker command envelopes, stream manifests, sample headers, session manifests, timing stamps, transport endpoints, heartbeat state, drop counters, replay records, deterministic synthetic stream payloads, and low-latency session/metric boundaries are reusable across engines and providers without embedding a Unity, Android, OpenXR, Makepad, LSL, OSC, vendor SDK, or video backend."
license_assumptions = "Authored for this MIT repository as clean-room public contracts. EDIA, RCAS, Kyber, WebRTC, QUIC, FFmpeg, libVLC, and vendor SDKs are treated as public behavioral references only unless separately licensed and attributed. No Kyber source, SDK headers, binaries, packet layouts, or proprietary implementation details are copied."
scrubbed_private_dependencies = ["downstream experiment semantics", "private stream names", "package identity", "generated captures", "device logs", "native SDK payloads", "Kyber SDK code", "Kyber packet layouts", "vendor binaries"]
review_status = "first-contract-pass-plus-transport-boundary"

[[utility]]
crate = "rusty-xr-eye-model"
source_family = "provider-neutral eye-data broker workflows"
extraction_date = "2026-05-09"
public_safe_rationale = "Screen-space gaze, XR gaze-ray, AOI hit, processor-event, validity, provenance, and deterministic synthetic eye samples are reusable contracts without bundling tracker SDKs, native runtimes, licenses, calibration UI, or app-specific experiment semantics."
license_assumptions = "Authored for this MIT repository as clean-room public contracts. Native tracker SDKs and data forwarding providers require separate license and field-of-use review before adapter work."
scrubbed_private_dependencies = ["device serials", "native tracker SDK binaries", "license keys", "calibration profiles", "participant data", "downstream AOI maps", "app-specific experiment semantics"]
review_status = "first-contract-pass"

[[utility]]
crate = "rusty-xr-debug-canvas"
source_family = "XR diagnostics panel layout workflows"
extraction_date = "2026-05-02"
public_safe_rationale = "Logical sections, badges, key/value rows, wrapping, tones, and normalized draw lists are reusable across headset-visible diagnostics without embedding a UI framework or renderer."
license_assumptions = "Authored for this MIT repository as a clean-room, dependency-light implementation of general XR panel layout concepts. The Quest composite example renderer uses JetBrains Mono under SIL OFL 1.1 and fontdue as a build-only rasterizer under MIT OR Apache-2.0 OR Zlib."
scrubbed_private_dependencies = ["downstream scene behavior", "framework widget code", "renderer backend code", "app-specific diagnostic fields"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-polar"
source_family = "public Polar H10 protocol references"
extraction_date = "2026-04-29"
public_safe_rationale = "Polar H10 GATT identifiers, HR/RR decoding, ECG/ACC frame contracts, PMD command builders, and public LSL schemas are reusable sensor utilities."
license_assumptions = "Authored for this MIT repository with attribution expected to the public Polar BLE SDK and MesmerPrism PolarH10 docs."
scrubbed_private_dependencies = ["downstream biofeedback behavior", "app-specific signal logic", "device pairing state"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-camera-model"
source_family = "public camera metadata and projection workflows"
extraction_date = "2026-04-29"
public_safe_rationale = "Intrinsics scaling, projection, back-projection, and timestamp matching are app-neutral camera utilities."
license_assumptions = "Authored for this MIT repository."
scrubbed_private_dependencies = ["native camera acquisition", "captured media", "platform texture handles"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-depth-model"
source_family = "Quest and OpenXR depth diagnostics"
extraction_date = "2026-04-29"
public_safe_rationale = "Depth readiness classification, per-view frame metadata, cadence summaries, and readback policies separate public status from platform provider ownership."
license_assumptions = "Authored for this MIT repository; public Makepad depth concepts are treated as behavioral reference only."
scrubbed_private_dependencies = ["native provider calls", "downstream depth captures", "scene-specific meshing behavior", "shader-language backend code"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-sdf"
source_family = "SDF and scan-fusion contract extraction"
extraction_date = "2026-04-29"
public_safe_rationale = "Packed SDF grids, sparse TSDF snapshots, scan samples, mesh snapshots, dynamic mesh-to-SDF reference conversion, and depth query surface summaries are reusable data contracts and utilities."
license_assumptions = "Authored for this MIT repository; public Makepad depth-query concepts are treated as behavioral reference only and algorithmic imports require separate license review."
scrubbed_private_dependencies = ["worker ownership", "captured room scans", "downstream physics adapters", "scene-specific query constants"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-particles"
source_family = "low-dependency particle experiments and Makepad-style instanced particle rendering concepts"
extraction_date = "2026-05-05"
public_safe_rationale = "Particle state, deterministic clocks, render payload conversion, backend-neutral billboard geometry, scene-basis billboard instance prep, sprite-mask atlas generation, trail buffers, render-budget estimates, depth sorting, mesh-surface coordinate sampling, live deformed-mesh anchor updates, same-surface neighbor tiers, cross-surface neighbor links, dynamic mesh collider helpers, and SDF surface-attraction helpers are reusable without downstream simulation semantics."
license_assumptions = "Authored for this MIT repository. Makepad is credited as an enabling public Rust-native rendering and UI foundation; this crate keeps helpers backend-neutral and does not vendor Makepad framework code."
scrubbed_private_dependencies = ["downstream simulation behavior", "downstream visual feedback behavior", "renderer backend code", "package identity", "generated captures"]
review_status = "particle-rendering-helper-pass"

[[utility]]
crate = "tools"
source_family = "public utility foundation"
extraction_date = "2026-04-29"
public_safe_rationale = "Docs link checks, schema export, and boundary scanning keep the public repo stable as examples are added."
license_assumptions = "Authored for this MIT repository."
scrubbed_private_dependencies = ["machine-specific scanner term lists", "local path rules", "pre-push machine hooks"]
review_status = "foundation-pass"

[[utility]]
crate = "rusty-xr-contracts/examples/plain_stereo_feedback_layout"
source_family = "synthetic public media-layout example"
extraction_date = "2026-04-29"
public_safe_rationale = "Demonstrates public layer and border layout contracts without native capture, APK packaging, headset hardware, or downstream app behavior."
license_assumptions = "Authored for this MIT repository."
scrubbed_private_dependencies = ["native capture implementation", "Quest APK integration", "downstream image-processing effects"]
review_status = "first-example"

[[utility]]
crate = "rusty-xr-contracts/examples/composite_feedback_session"
source_family = "synthetic public session-contract example"
extraction_date = "2026-04-29"
public_safe_rationale = "Demonstrates layer layout, capture lifecycle state, room/depth diagnostics, and companion catalog metadata without native capture or APK payloads."
license_assumptions = "Authored for this MIT repository."
scrubbed_private_dependencies = ["runtime permission implementation", "native capture implementation", "APK build output", "downstream visual effects"]
review_status = "synthetic-example"

[[utility]]
crate = "examples/quest-minimal-apk"
source_family = "public authored APK smoke test"
extraction_date = "2026-04-29"
public_safe_rationale = "Provides the first public APK-producing Rusty XR example while keeping native capture and OpenXR adapters deferred."
license_assumptions = "Authored for this MIT repository."
scrubbed_private_dependencies = ["private package identity", "downstream app shell", "private visual effects", "signing material"]
review_status = "public-apk-smoke-test"

[[utility]]
crate = "rusty-xr-contracts/room"
source_family = "public room mesh and capture lifecycle contract extraction"
extraction_date = "2026-04-29"
public_safe_rationale = "Room mesh state, semantic surface ranges, and capture lifecycle metadata are shared by scan, depth, diagnostics, examples, and operator tooling."
license_assumptions = "Authored for this MIT repository from public XR data-model concepts and local workflow requirements."
scrubbed_private_dependencies = ["native provider implementation", "generated room captures", "downstream scene behavior"]
review_status = "foundation-pass"

[[utility]]
crate = "tools/schema/quest-app-catalog"
source_family = "public companion/operator catalog metadata"
extraction_date = "2026-04-29"
public_safe_rationale = "A stable catalog schema lets public example APK metadata flow to companion tools without sharing APK bytes or build systems."
license_assumptions = "Authored for this MIT repository and aligned with the public Rusty XR Companion Apps catalog shape."
scrubbed_private_dependencies = ["private package identities", "local APK paths", "signing material", "release payload bytes"]
review_status = "foundation-pass"

[[utility]]
crate = "examples/quest-broker-apk"
source_family = "public authored Quest broker proof"
extraction_date = "2026-05-02"
public_safe_rationale = "A small sidecar APK demonstrates localhost status/WebSocket contracts, latency sample forwarding, optional LSL packaging, and OSC ingress/egress without publishing downstream app behavior."
license_assumptions = "Authored for this MIT repository. Native liblsl is optional, external, and must be supplied with matching downstream license notices when packaged."
scrubbed_private_dependencies = ["private package identity", "downstream Unity project", "local liblsl paths", "validation device addresses", "generated APK bytes", "signing material"]
review_status = "public-proof"

[[utility]]
crate = "crates/rusty-xr-particles/hand-mesh-live-sampler"
source_family = "public Rust hand mesh particle utility"
extraction_date = "2026-05-05"
public_safe_rationale = "A framework-neutral live sampler consumes public HandMeshSnapshot frames, maintains stable particle anchors while deformed vertices update, and rebuilds only when topology changes. Native platform calls remain in optional adapters or downstream shells."
license_assumptions = "Authored for this MIT repository without copying platform SDK samples or downstream app behavior."
scrubbed_private_dependencies = ["native provider implementation", "package identity", "private scene behavior", "app-specific streams", "generated captures", "signing material"]
review_status = "public-utility"

[[utility]]
crate = "crates/rusty-xr-particles/dynamic-mesh-coordinate-sampler"
source_family = "public dynamic mesh and hand-mesh interaction utility"
extraction_date = "2026-05-06"
public_safe_rationale = "Stable barycentric coordinate sampling, same-surface neighbor tiers, cross-surface neighborhood links, and live deformed-mesh updates are reusable for hands, scanned meshes, procedural meshes, and interaction passes. The public shape keeps native Meta/OpenXR calls in adapters or examples."
license_assumptions = "Authored for this MIT repository from public mesh-sampling and OpenXR hand-mesh concepts; no platform SDK sample code or downstream behavior is copied."
scrubbed_private_dependencies = ["native provider implementation", "renderer backend code", "private coupling behavior", "package identity", "generated captures", "signing material"]
review_status = "public-utility"

[[utility]]
crate = "crates/rusty-xr-sdf/dynamic-mesh-to-sdf"
source_family = "public dynamic mesh field utility"
extraction_date = "2026-05-06"
public_safe_rationale = "A dependency-light CPU mesh-to-SDF builder and SDF particle attraction example are reusable for hands, scanned meshes, procedural meshes, collision fields, and diagnostics. The public shape keeps worker scheduling, GPU kernels, native providers, renderer buffers, and app-specific force behavior downstream."
license_assumptions = "Authored for this MIT repository from public SDF and triangle-mesh geometry concepts; no platform SDK sample code or downstream behavior is copied."
scrubbed_private_dependencies = ["native provider implementation", "worker ownership", "renderer backend code", "private coupling behavior", "package identity", "generated captures", "signing material"]
review_status = "public-utility"

[[utility]]
crate = "crates/rusty-xr-particles/dynamic-mesh-collider"
source_family = "public dynamic mesh collider utility"
extraction_date = "2026-05-06"
public_safe_rationale = "Collider-ready dynamic mesh surfaces, optional normal-based inflation, diagnostic visual shells, and simple closest-point/sphere-overlap probes are reusable for hands, scan meshes, fixture meshes, and adapter-owned physics integrations without pulling engine or platform SDK code into core."
license_assumptions = "Authored for this MIT repository from public triangle-mesh collision and diagnostic-visual workflow concepts; no platform SDK sample code or downstream behavior is copied."
scrubbed_private_dependencies = ["native provider implementation", "physics-engine handles", "renderer backend code", "collision callbacks", "package identity", "generated captures", "signing material"]
review_status = "public-utility"
