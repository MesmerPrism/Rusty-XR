[package]
name = "rusty-xr-camera-model"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Camera metadata and projection helpers for Rusty XR."

[features]
default = []
serde = ["dep:serde", "rusty-xr-contracts/serde"]

[dependencies]
rusty-xr-contracts = { path = "../rusty-xr-contracts" }
serde = { workspace = true, optional = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
