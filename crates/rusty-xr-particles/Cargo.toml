[package]
name = "rusty-xr-particles"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "General particle and animation primitives for Rusty XR."

[features]
default = []
serde = ["dep:serde", "rusty-xr-contracts/serde"]

[dependencies]
rusty-xr-contracts = { path = "../rusty-xr-contracts" }
serde = { workspace = true, optional = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
